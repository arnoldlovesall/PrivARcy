# Legacy frontend docs

These two files (`CHANGES.md`, `IMPLEMENTATION_SUMMARY.md`) describe the
old PyQt5 GUI frontend, which has been removed. PrivARcy is now a pure
backend service — see `../../ARCHITECTURE.md` and `../../backend/README.md`
for the current system. Kept here only as historical reference for what
each GUI page used to do.
