# Compatible runtime dependencies for the bundled Python 3.11 backend.
# Kept separate from the historical freeze, which pins NumPy 2.x although
# OpenCV 4.8 wheels require the NumPy 1.x ABI on this environment.
numpy==1.26.4
opencv-python==4.8.1.78
face-recognition==1.3.0
ultralytics==8.4.137
Pillow==12.3.0

# Pure-backend API layer (replaces PyQt5, which the GUI frontend used)
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# TrOCR text extraction (replaces pytesseract)
torch==2.5.1
transformers==4.47.1

# Live.py camera device naming (Windows only) — enumerates DirectShow
# devices in the same order cv2.VideoCapture(index, cv2.CAP_DSHOW) does,
# so the dropdown shows real device names instead of the backend API name.
pygrabber==0.2; sys_platform == "win32"
